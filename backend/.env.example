# Optional — only needed for AI-generated athlete bios & news filtering.
# Without it, athlete data, matching, and business metrics all still work.
GROQ_API_KEY=your_groq_api_key_here

# Production CORS allow-list (comma-separated). Add your deployed frontend URL.
# Example: ALLOWED_ORIGINS=https://my-match-olympics.vercel.app
ALLOWED_ORIGINS=
